version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./db.sqlite:/app/db.sqlite
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
